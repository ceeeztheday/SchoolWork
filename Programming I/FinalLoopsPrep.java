import java.util.*;
public class FinalLoopsPrep
{
   public static void main (String [] args)
   
   {
         //This program prints out the elements in an array 5 times.
      
      //variable declaration
      
      int [] a = {1, 2, 3, 4, 5};
      int row;
      int col;
      
         
   // this program prints numbers in the array once (using string b)
   for(col = 0; col < b.length; col++)
   System.out.print(b[col] + "\t");
   
   // this loops ends
   
   
  // new loop using array a multiplied by row
  
  for(row = 0; row < 5; row++)
  {
  for(col = 0; col < a.length; col++)
   {
   System.out.print(a[col] * row + "\t");
   }
  }
   // this loop ends
   
   
   //for loop multiplying one array by the other
   for(row = 0; row < val.length; row++)
      {
         for(col = 0; col < a.length; col++)
         {
         System.out.print(a[col] * val[row]);
         }
       }
       System.out.println();
       
       // this loop ends
       
    
       
      
   }
}
